{
  "name": "morse-code-cheatsheet",
  "version": "1.0.4",
  "description": "A static HTML page project with Semantic Release",
  "main": "index.html",
  "repository": {
    "type": "git",
    "url": "https://github.com/ikatkov/morse-code.git"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/npm": "^12.0.1",
    "semantic-release": "^24.2.0"
  },
  "scripts": {
    "postversion": "sed -i '' 's/__VERSION__/'\"$npm_package_version\"'/g' manifest.json && git add manifest.json"
  }
}
